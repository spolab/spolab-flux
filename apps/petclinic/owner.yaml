apiVersion: apps/v1
kind: Deployment
metadata:
  name: petclinic-owner
  namespace: petclinic
spec:
  selector:
    matchLabels:
      app: petclinic-owner
  template:
    metadata:
      labels:
        app: petclinic-owner
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: petclinic-owner
        dapr.io/app-port: "3000"
    spec:
      containers:
      - name: petclinic-owner
        image: spolab/petclinic-owner:v0.0.2
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 3000