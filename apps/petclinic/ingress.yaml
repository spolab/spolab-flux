apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: petclinic
  namespace: petclinic
  labels:
    name: petclinic
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web, websecure
    dapr.io/enabled: "true"
spec:
  rules:
  - host: petclinic.127.0.0.1.nip.io
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: petclinic-owner-dapr
            port: 
              name: dapr-http